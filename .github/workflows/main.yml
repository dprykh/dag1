name: Post-Merge Script

on:
  push:
     branches:
      - main

jobs:
  post_merge:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Debugging
      run: |
        ls -al
        git log --oneline -n 2

    - name: Run Post-Merge Script
      run: |
            file_path = "src/python/airflow_dags/tuto.py"
            sed "10s/.*/Last Git commit: AAAAAA/" $file_path > temp && mv temp $file_path
